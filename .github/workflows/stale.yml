name: Stale

on:
    schedule:
        - cron: '0 6 * * *'

jobs:
    stale:
        uses: apermo/reusable-workflows/.github/workflows/reusable-stale.yml@main
        permissions:
            issues: write
            pull-requests: write
